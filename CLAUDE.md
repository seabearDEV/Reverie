Follow the agent contract in @AGENTS.md — one contract for every agent in this repo (fleet policy: shell-capable agents use the `rvr` CLI; the MCP server is for shell-less surfaces only, registered app-level, never via project `.mcp.json`).
